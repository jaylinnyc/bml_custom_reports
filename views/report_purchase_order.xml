<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- ======================================================= -->
        <!-- 1. FORM VIEW EXTENSION (Signatures below Terms & Conditions) -->
        <!-- ======================================================= -->
        <record id="view_purchase_order_form_inherit_signatures" model="ir.ui.view">
            <field name="name">purchase.order.form.inherit.signatures</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <!-- TARGET: The 'notes' field (Terms and Conditions) -->
                <!-- ACTION: Insert AFTER it -->
                <xpath expr="//page[@name='products']/div[hasclass('clearfix')]" position="before">
                    
                    <group string="Signatures &amp; Approvals" name="signatures_group">
                        
                        <!-- REQUESTER: Only visible in 'draft' state (RFQ) -->
                        <group string="Requester" invisible="state != 'draft'">
                            <field name="x_studio_requester_printed_name" placeholder="e.g. John Doe"/>
                            <field name="x_studio_requester_signature" widget="signature"/>
                            <field name="x_studio_request_date"/>
                        </group>
                        
                        <!-- APPROVER: Visible always -->
                        <group string="Approver">
                            <field name="x_studio_approval_signature" widget="signature"/>
                            <!-- Standard date_approve is usually read-only or handled by workflow -->
                            <field name="date_approve" readonly="1"/>
                        </group>
                    </group>

                </xpath>
            </field>
        </record>

        <!-- Add server action to print RFQ report from Action menu -->
        <record id="action_print_as_rfq" model="ir.actions.server">
            <field name="name">Print PO Quotation</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_model_id" ref="purchase.model_purchase_order"/>
            <field name="binding_view_types">form</field>
            <field name="state">code</field>
            <field name="code">
if record.state not in ('purchase', 'done'):
    raise UserError("This action is only available for confirmed purchase orders.")
action = env.ref('purchase.report_purchase_quotation').report_action(record)
            </field>
        </record>

        <!-- ======================================================= -->
        <!-- 1. PURCHASE ORDER (Confirmed) - Signature Left / Totals Right -->
        <!-- ======================================================= -->
        <template id="bml_po_signature_side_by_side"
                  inherit_id="purchase.report_purchaseorder_document"
                  name="BML - PO Signature left + Totals right"
                  priority="50">

            <xpath expr="//div[hasclass('page')]" position="before">
                <t t-set="bml_doc_type" t-value="'po'"/>
            </xpath>

            <!-- Remove 'Payment Terms:' label -->
            <xpath expr="//p[@t-field='o.note']/following::strong[1]" position="replace"/>
            <!-- Remove the actual payment term value (e.g. '30 Days') -->
            <xpath expr="//p[@t-field='o.note']/following::span[@t-field='o.payment_term_id']" position="replace"/>

            <!-- 1. PARENT: Clear floats -->
            <xpath expr="//div[@id='total']" position="attributes">
                <attribute name="class">mt-4 clearfix</attribute>
                <attribute name="style">min-height: 150px;</attribute>
            </xpath>

            <!-- 2. TOTALS (RIGHT): Force Float Right -->
            <xpath expr="//div[@id='total']/div[1]" position="attributes">
                <attribute name="class">float-end</attribute>
                <attribute name="style">float: right; width: 50%; min-width: 300px; margin: 0 !important; padding: 0;</attribute>
            </xpath>

            <!-- 3. SIGNATURE & DATE (LEFT): Split into 2 columns -->
            <xpath expr="//div[@id='total']/div[1]" position="before">
                <div class="float-start" style="float: left; width: 48%; padding-right: 10px; box-sizing: border-box;">
                    <div class="row m-0">
                        <!-- Signature Col -->
                        <div class="col-6 text-center px-1">
                            <div style="height: 80px; display: flex; align-items: flex-end; justify-content: center;">
                                <t t-if="o.x_studio_approval_signature">
                                    <img t-att-src="image_data_uri(o.x_studio_approval_signature)" 
                                         style="max-height: 75px; width: auto; object-fit: contain; margin-bottom: 2px;"/>
                                </t>
                            </div>
                            <div style="border-top: 1px solid black; width: 100%; margin: 5px auto;"/>
                            <span style="font-size: 11px;">ผู้อนุมัติ / Approved by</span>
                        </div>
                        <!-- Date Col -->
                        <div class="col-6 text-center px-1">
                            <div style="height: 80px; position: relative;">
                                <div style="position: absolute; bottom: 3px; left: 0; right: 0; width: 100%;">
                                    <t t-if="o.date_approve">
                                        <strong style="font-size: 14px;">
                                            <span t-field="o.date_approve" t-options="{&quot;widget&quot;: &quot;date&quot;}"/>
                                        </strong>
                                    </t>
                                    <t t-else="">
                                        <span style="color: #eee;">.</span> 
                                    </t>
                                </div>
                            </div>
                            <div style="border-top: 1px solid black; width: 100%; margin: 5px auto;"/>
                            <span style="font-size: 11px;">วันที่อนุมัติ / Approval Date</span>
                        </div>
                    </div>
                </div>
            </xpath>
        </template>


        <!-- ======================================================= -->
        <!-- 2. REQUEST FOR QUOTATION (RFQ) - Dual Signatures        -->
        <!-- ======================================================= -->
        <template id="bml_rfq_signature_block"
                  inherit_id="purchase.report_purchasequotation_document"
                  name="BML - RFQ Signature Block (Requester + Approver)"
                  priority="50">
            
            <xpath expr="//div[hasclass('page')]" position="before">
                <t t-set="bml_doc_type" t-value="'rfq'"/>
            </xpath>

            <!-- Insert at the end of the page content -->
            <xpath expr="//div[hasclass('page')]" position="inside">
                <div class="mt-5" style="page-break-inside: avoid;">
                    
                    <div class="row">
                        <!-- LEFT COLUMN: REQUESTER (Purchase Representative) -->
                        <div class="col-6">
                            <div class="text-center" style="width: 90%; margin: 0 auto;">
                                
                                <!-- REQUESTER SIGNATURE IMAGE -->
                                <div style="height: 80px; display: flex; align-items: flex-end; justify-content: center;">
                                    <t t-if="o.x_studio_requester_signature">
                                        <img t-att-src="image_data_uri(o.x_studio_requester_signature)" 
                                             style="max-height: 75px; width: auto; object-fit: contain; margin-bottom: 2px;"/>
                                    </t>
                                    <t t-else="">
                                        <!-- Placeholder -->
                                        <span style="color: #eee;">.</span>
                                    </t>
                                </div>
                                
                                <!-- Line -->
                                <div style="border-top: 1px solid black; width: 100%; margin: 5px auto;"/>
                                
                                <!-- Details -->
                                <span style="font-size: 11px;">ผู้ขอซื้อ / Requested by</span>
                                <br/>
                                
                                <!-- Use PRINTED NAME field -->
                                <t t-if="o.x_studio_requester_printed_name">
                                    <span t-field="o.x_studio_requester_printed_name" style="font-weight: bold;"/>
                                </t>
                                <t t-else="">
                                    <!-- Fallback to blank line if they haven't typed it yet -->
                                    <span>____________________</span>
                                </t>

                                <br/>
                                <span style="font-size: 11px;">วันที่ / Date: </span>
                                
                                <!-- Logic: Use x_studio_request_date if set, otherwise fallback to date_order -->
                                <t t-if="o.x_studio_request_date">
                                    <span t-field="o.x_studio_request_date" t-options='{"widget": "date"}'/>
                                </t>
                                <t t-else="">
                                    <span t-field="o.date_order" t-options='{"widget": "date"}'/>
                                </t>
                            </div>
                        </div>

                        <!-- RIGHT COLUMN: APPROVER (Manager) -->
                        <div class="col-6">
                            <div class="text-center" style="width: 90%; margin: 0 auto;">
                                <!-- Image Holder -->
                                <div style="height: 80px; display: flex; align-items: flex-end; justify-content: center;">
                                    <t t-if="o.x_studio_approval_signature">
                                        <img t-att-src="image_data_uri(o.x_studio_approval_signature)" 
                                             style="max-height: 75px; width: auto; object-fit: contain; margin-bottom: 2px;"/>
                                    </t>
                                    <t t-else="">
                                        <span style="color: #eee;">.</span>
                                    </t>
                                </div>

                                <!-- Line -->
                                <div style="border-top: 1px solid black; width: 100%; margin: 5px auto;"/>

                                <!-- Details -->
                                <span style="font-size: 11px;">ผู้อนุมัติ / Approved by</span>
                                <br/>
                                <!-- If approved, show date, else show placeholder line or empty -->
                                <t t-if="o.date_approve">
                                    <span style="font-size: 11px;">วันที่ / Date: </span>
                                    <span t-field="o.date_approve" t-options='{"widget": "date"}'/>
                                </t>
                                <t t-else="">
                                    <br/>
                                    <span style="font-size: 11px;">วันที่ / Date: _________________</span>
                                </t>
                            </div>
                        </div>
                    </div>

                </div>
            </xpath>
        </template>


        <!-- ======================================================= -->
        <!-- 3. ADDRESS FIX (Removing Top Padding)                   -->
        <!-- ======================================================= -->
        <template id="bml_address_padding_fix"
                  inherit_id="web.address_layout"
                  name="BML - Remove address top padding"
                  priority="100">
            <xpath expr="//div[@t-if=&quot;address&quot;]" position="attributes">
                <attribute name="style">padding-top: 0 !important; margin-top: 0 !important;</attribute>
                <attribute name="class">address row mb-0</attribute>
            </xpath>
        </template>


        <!-- ======================================================= -->
        <!-- 4. FOOTER (Purchase Order & Tax Invoice)                -->
        <!-- ======================================================= -->
        <template id="external_layout_boxed_footer"
                  inherit_id="web.external_layout_boxed"
                  name="Custom ISO Footer - Purchase Order &amp; Tax Invoice">
            <xpath expr="//div[contains(@class, 'footer')]/div[not(@t-if) and not(@t-else)]" position="inside">
                <t t-set="model_name" t-value="o._name if o else False"/>
                
                <!-- Purchase Order Footer -->
                <t t-if="model_name == 'purchase.order'">
                    <!-- Determine report type based on bml_doc_type variable -->
                    <t t-set="footer_report_type" t-value="'purchase_order_rfq' if bml_doc_type == 'rfq' else 'purchase_order_bill'"/>
                    
                    <!-- Fetch footer configuration from database -->
                    <t t-set="footer_config" t-value="request.env['bml.report.footer.config'].sudo().search([
                        ('report_type', '=', footer_report_type),
                        ('company_id', '=', o.company_id.id),
                        ('active', '=', True)
                    ], limit=1)"/>
                    
                    <!-- Only display footer if configuration exists -->
                    <t t-if="footer_config">
                        <div class="w-100"></div>
                        <div class="row">
                            <!-- Left Column: Storage/Retention Notes -->
                            <t t-if="footer_config.footer_left">
                                <div class="col-6 text-start">
                                    <small class="text-muted font-weight-medium" t-esc="footer_config.footer_left"/>
                                </div>
                            </t>
                            
                            <!-- Right Column: ISO Code and Metadata -->
                            <t t-if="footer_config.footer_right">
                                <div t-attf-class="col-#{6 if footer_config.footer_left else 12} text-end">
                                    <small class="text-muted font-weight-medium" t-esc="footer_config.footer_right"/>
                                </div>
                            </t>
                        </div>
                    </t>
                </t>
                
                <!-- Tax Invoice Footer (Sales Invoice) -->
                <t t-if="model_name == 'account.move' and o.move_type == 'out_invoice'">
                    <!-- Fetch footer configuration for tax invoice -->
                    <t t-set="footer_config" t-value="request.env['bml.report.footer.config'].sudo().search([
                        ('report_type', '=', 'sales_tax_invoice'),
                        ('company_id', '=', o.company_id.id),
                        ('active', '=', True)
                    ], limit=1)"/>
                    
                    <!-- Only display footer if configuration exists -->
                    <t t-if="footer_config">
                        <div class="w-100"></div>
                        <div class="row">
                            <!-- Left Column: Storage/Retention Notes -->
                            <t t-if="footer_config.footer_left">
                                <div class="col-6 text-start">
                                    <small class="text-muted font-weight-medium" t-esc="footer_config.footer_left"/>
                                </div>
                            </t>
                            
                            <!-- Right Column: ISO Code and Metadata -->
                            <t t-if="footer_config.footer_right">
                                <div t-attf-class="col-#{6 if footer_config.footer_left else 12} text-end">
                                    <small class="text-muted font-weight-medium" t-esc="footer_config.footer_right"/>
                                </div>
                            </t>
                        </div>
                    </t>
                </t>
            </xpath>
        </template>
        
    </data>
</odoo>