<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Manual Auto-Reconcile action to Bank Reconciliation Widget (Kanban View) -->
    <record id="action_bank_statement_line_manual_reconcile" model="ir.actions.server">
        <field name="name">Auto Reconcile</field>
        <field name="model_id" ref="account.model_account_bank_statement_line"/>
        <field name="binding_model_id" ref="account.model_account_bank_statement_line"/>
        <field name="binding_view_types">kanban,list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_manual_auto_reconcile()
        </field>
    </record>
</odoo>
